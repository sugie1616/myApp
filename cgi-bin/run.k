#!/usr/local/bin/konoha

OUT << "Content-Type: text/plain" << EOL << EOL;

String query = $env.QUERY_STRING;

String outputResult(String kScript) {
	String result;
	/* 
	run kScript
	*/
	return result;
}
